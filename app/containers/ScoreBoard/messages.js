/*
 * ScoreBoard Messages
 *
 * This contains all the text for the ScoreBoard component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.ScoreBoard.header',
    defaultMessage: 'This is ScoreBoard container !',
  },
});
